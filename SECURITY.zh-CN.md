# 安全说明

`agent-which` 是本地 CLI。它不会调用外部 API，不会执行指令文件，也不会上传仓库内容。

## 报告方式

如果发现安全问题，请优先在 GitHub 上创建 private advisory；不可用时可直接联系维护者。

## 范围

安全相关问题包括：

- 读取请求仓库根目录之外的文件
- 执行不可信工作区内容
- 泄露环境变量或密钥值

指令质量判断和提示注入检测不属于本工具范围。
